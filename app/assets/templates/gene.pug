extend header

block content 
  if data
    each item in data
      if item._id
        p
          a(href='/gene/' + item._id) #{item.species}
      else
        pre=JSON.stringify(locals, null, 4)
  if error
    pre=error

